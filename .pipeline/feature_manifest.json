{
  "_behavioral_specs": {
    "_global": [
      {
        "text": "## Summary\nComplete the already-scaffolded NestJS + Angular + Prisma/PostgreSQL FaithfulE app by adding backend Notes CRUD, wiring the Angular UI to the real API, aligning auth/health routes, correcting seed emails, and packaging it as a single container that serves the SPA and API on port 8080.\n\n## Scope\n**Decision — build on the scaffolded NestJS + Angular templates, not the Express/React \"planned stack\" in ATLAS_STACK.md.** Verdict is GREENFIELD, but the concrete scaffold already implements this exact app: Angular has login/notes-list/note-editor/admin routes with the mandatory colossus `data.flow` flow-graph metadata (Angular-specific platform infra), \"FaithfulE\" branding, and the `app-ready` testid; NestJS has JWT auth, user, health, Prisma, and the SEED_CRED entrypoint. The Express/React text is a superseded earlier scope note; rewriting would discard working, platform-integrated code. Only Notes CRUD, API wiring, and single-container serving are missing.\n\nAll paths below are under `.pipeline-worktrees/pipeline-2026-07-13-83910f44/`.\n\n### Files to modify\n- `backend/prisma/schema.prisma` — add `Note` model (`id`, `title`, `body`, `done` default false, `userId` FK→User, `createdAt`, `updatedAt`) + `notes Note[]` relation on `User`.\n- `backend/prisma/seed/seed.ts` (and rebuilt `seed.js`) — change seeded emails to `admin@faithful-e.test` / `user@faithful-e.test`; keep deterministic `derivePassword` + `SEED_CRED <role> <email> <password>` print.\n- `backend/src/main.ts` — listen on `process.env.PORT ?? 8080`; change global prefix from `api/v1` to `api`; keep helmet/validation.\n- `backend/src/app.module.ts` — import new `NotesModule` and `ServeStaticModule` (serve Angular build from `client/`, `exclude: ['/api/{*path}']`).\n- `backend/src/health/health.controller.ts` — expose liveness at `/api/health` (`@Get()`) and readiness at `/api/health/deep` (`@Get('deep')`, Prisma ping); keep `@Public()`.\n- `backend/src/auth/auth.controller.ts` — remove the public `register` endpoint (login-only per ATLAS rule); keep `login` and `refresh-token`.\n- `backend/package.json` — add `@nestjs/serve-static`.\n- `web/frontend/src/environments/environment.ts` & `environment.prod.ts` — set `apiUrl: '/api'` (relative; same-origin + ingress-safe).\n- `web/frontend/src/app/core/services/auth.service.ts` — replace mock `login()` with real `POST /api/auth/login`; normalize returned `role` to uppercase (`ADMIN`/`USER`) to match frontend `UserRole`; persist `{token,user}`. Keep `demoLogin` optional.\n- `web/frontend/src/app/features/notes/notes-list.component.ts` — inject `NotesService`; load via `GET /api/notes` in `ngOnInit`; `toggleDone`→`PATCH /api/notes/:id`; `deleteNote`→`DELETE /api/notes/:id`; drop mock array.\n- `web/frontend/src/app/features/notes/note-editor.component.ts` — load `GET /api/notes/:id`; `save()`→`POST /api/notes` (create) or `PATCH /api/notes/:id` (edit); `remove()`→`DELETE`.\n- `Dockerfile` (repo root — currently an nginx placeholder) — replace with multi-stage single image (see Step 8).\n\n### Files to create\n- `backend/src/notes/notes.module.ts`, `notes.controller.ts`, `notes.service.ts`\n- `backend/src/notes/dto/create-note.dto.ts`, `update-note.dto.ts` (class-validator; `PartialType` for update)\n- `backend/prisma/migrations/<ts>_init/migration.sql` — initial migration covering `User` + `Note` (required for `prisma migrate deploy`).\n- `web/frontend/src/app/core/services/notes.service.ts` — typed CRUD against `environment.apiUrl + '/notes'`.\n\n### Dependencies\n- `@nestjs/serve-static` (backend). No new frontend deps. No third-party external services.\n\n### Assumptions\n- API prefix standardized to `/api`; health at `/api/health` + `/api/health/deep`; auth at `/api/auth/login` — matches ATLAS public-route list.\n- Notes are per-user scoped (owner = JWT `sub`/`id`); all `/api/notes*` routes behind existing `@Auth()`/JWT guard.\n- Public routes: `/login`, `/api/auth/login`, `/api/health`, `/api/health/deep`. No public signup (register removed).\n- Backend Prisma `Role` stays lowercase (`admin|user`); frontend role casing bridged in `auth.service`.\n- Single container: NestJS serves Angular build (`web/frontend/dist/frontend`, per `angular.json` outputPath) on `PORT` 8080; `docker-entrypoint.sh` (migrate deploy → seed → `node dist/main`) reused.\n- `admin/settings` remains scaffolded mock (outside spec scope), route left intact and non-breaking.\n- Auth model = full_auth with seeded users only; route guards already applied via `authGuard` on the shell.\n\n## Implementation Plan\n\n### Step 1: Add Note model + migration\n**File:** `backend/prisma/schema.prisma`\nAdd `Note` model with `userId` FK and back-relation on `User`. Generate the initial migration (User + Note) so `prisma migrate deploy` works on a fresh DB. Run `prisma generate` (output `src/generated/prisma`).\n\n### Step 2: Notes backend module\n**Files:** `backend/src/notes/*`\nCreate `NotesService` (Prisma CRUD scoped by `userId`), `NotesController` (`GET /`, `POST /`, `GET /:id`, `PATCH /:id`, `DELETE /:id`) under `@Controller('notes')`, decorated with `@Auth()` and `@GetUser()` to obtain the current user. DTOs validate `title` (required, maxLength 120), `body` (required), `done` (boolean). Register in `app.module.ts`.\n\n### Step 3: Fix seed emails\n**File:** `backend/prisma/seed/seed.ts`\nSet `SEED_USERS` emails to `admin@faithful-e.test` / `user@faithful-e.test`; rebuild `seed.js`. Verify `SEED_CRED` line format unchanged.\n\n### Step 4: Align server routes & port\n**Files:** `backend/src/main.ts`, `backend/src/health/health.controller.ts`\nGlobal prefix `api`; listen on `PORT ?? 8080`. Health → `/api/health` (liveness) and `/api/health/deep` (Prisma readiness).\n\n### Step 5: Login-only + static serving\n**Files:** `backend/src/auth/auth.controller.ts`, `backend/src/app.module.ts`, `backend/package.json`\nRemove `register`. Add `ServeStaticModule.forRoot({ rootPath: <client>, exclude: ['/api/{*path}'] })` so Angular deep links fall back to `index.html`.\n\n### Step 6: Angular auth wiring\n**File:** `web/frontend/src/app/core/services/auth.service.ts`\nReal `POST /api/auth/login`; map response `{user:{id,email,role},token}`, uppercasing `role`; persist to signals/localStorage. `environment*.ts` → `apiUrl:'/api'`.\n\n### Step 7: Angular notes wiring\n**Files:** `notes.service.ts`, `notes-list.component.ts`, `note-editor.component.ts`\nCreate `NotesService` and replace mock signals with live API calls (list/get/create/update/delete/toggle). Preserve existing signals, loading/error states, and routes/flow-meta.\n\n### Step 8: Single-image Dockerfile\n**File:** `Dockerfile` (repo root)\nMulti-stage: (1) build Angular (`web/frontend` → `dist/frontend`); (2) build backend (pnpm install, `prisma generate`, `pnpm build`); (3) runtime node:22-alpine — copy backend `dist`, `node_modules`, `prisma`, generated client, `docker-entrypoint.sh`, and Angular build into the ServeStatic `client/` path; `ENV PORT=8080`; `EXPOSE 8080`; `ENTRYPOINT [\"./docker-entrypoint.sh\"]`.\n\n### Step 9: Verify branding\nConfirm login page (`login.component.html`) and header render \"FaithfulE\" and `index.html` `<title>` is \"FaithfulE\" — already present in scaffold; no change expected.\n\n## Testing Strategy\n- `prisma migrate deploy` + seed on boot prints two `SEED_CRED ... @faithful-e.test` lines.\n- `GET /api/health` → 200; `GET /api/health/deep` → DB-ok JSON.\n- `POST /api/auth/login` with a seeded credential returns `{user,token}`; wrong creds → 400/401.\n- Authenticated notes CRUD via UI: create → appears in list; edit/toggle done persists; delete removes; notes isolated per user (login as user vs admin).\n- Unauthenticated access to app routes redirects to `/login`; token attached by interceptor; 401 clears session.\n- Docker image runs on 8080; SPA deep link (`/notes/new`) served with fallback; header + tab title show \"FaithfulE\"; `data-testid=\"app-ready\"` present; no reject signatures (\"Cannot GET /\", \"Loading...\").\n\n## Risks\n- `prisma migrate deploy` needs committed migration files — Step 1 must generate them (empty migrations dir currently).\n- Role casing mismatch (backend `admin` vs frontend `ADMIN`) — must bridge in `auth.service`, or admin nav/guard breaks.\n- ServeStatic `exclude` must not swallow `/api/*`; Angular output path is `dist/frontend` (flat, per `angular.json`).\n- Prisma v7 driver-adapter (`@prisma/adapter-pg`) reads the DB URL via adapter; ensure `DATABASE_URL` present for CLI migrate at boot.",
        "type": "architectural",
        "source": "plan",
        "rev": 0,
        "superseded": false
      }
    ]
  },
  "_component_requirements": {}
}