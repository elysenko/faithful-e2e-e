# Atlas Stack Status
Generated by scaffolder. Read this before writing any code.

## Verdict: EXISTING — preserve current stack

## Detected stack
- Runtime: NestJS (TypeScript) — backend/ ; Angular 19 (TypeScript) — web/frontend/
- ORM: Prisma (v7) with PostgreSQL
- Auth: Custom JWT (passport-jwt, @nestjs/jwt) — seeded users only, no public registration
- Health checks: present at /api/health (liveness) and /api/health/deep (readiness)
- K8s service name: none detected

## Rules for all agents
- Do not migrate the ORM
- Do not replace the auth implementation
- Do not change the framework
- Only add features within the existing patterns
- Backend lives at backend/; Angular frontend lives at web/frontend/
- API prefix is /api (not /api/v1)
- Port is 8080 (process.env.PORT ?? 8080)
- Notes API routes under /api/notes, JWT-guarded
- Do not add public registration — login-only with seeded credentials
- Angular environments must use apiUrl: '/api' (relative, same-origin)
